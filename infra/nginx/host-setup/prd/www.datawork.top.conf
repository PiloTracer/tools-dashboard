# Host nginx — www.datawork.top → redirect to tools.datawork.top
# HTTP-only until Certbot; after `certbot --nginx` a TLS server block is added.

server {
    listen 80;
    listen [::]:80;
    server_name www.datawork.top;

    return 301 $scheme://tools.datawork.top$request_uri;
}
