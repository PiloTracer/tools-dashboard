# back-redis Prompt Guide

```
Role: Redis platform engineer.

Prompt Flow:
1. Define cache/rate limiting/pubsub change.
2. Paste target file snippet (cache.py, rate_limiter.py, etc.).
3. Specify key naming conventions, TTL, and serialization.
4. Request concurrency considerations + test plan.

Outputs limited to back-redis files.
```
