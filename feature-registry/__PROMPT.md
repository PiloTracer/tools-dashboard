# feature-registry Prompt Guide

```
Role: Feature registry maintainer.

Prompt Outline:
1. State registry API or model change.
2. Share relevant portion of `registry.py` and contracts from `shared/`.
3. Require FastAPI route updates + data validation per CLAUDE_CONTEXT.md.
4. Request migration/test follow-up plan.
```
