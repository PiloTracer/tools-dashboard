name: auto-auth
version: 1.0.0
description: OAuth client and API key management (admin UI)
service: front-admin

routes:
  - path: /oauth-clients
    file: oauth-clients.tsx
    description: OAuth client management
  - path: /api-keys
    file: api-keys.tsx
    description: API key management

dependencies:
  - back-api (OAuth management APIs)
