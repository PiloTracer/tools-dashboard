name: two-factor
version: 1.0.1
description: Two-factor authentication API surface (minimal scaffold)
status: active
owner: Engineering Team

services:
  back-auth:
    router_prefix: /auth/two-factor
    endpoints:
      - path: /auth/two-factor/challenge
        method: POST
        description: Placeholder challenge endpoint (returns JSON status only)
        auth_required: false

    implementation:
      - back-auth/features/two-factor/api.py
      - back-auth/features/two-factor/domain.py

  front-public:
    notes:
      - No dedicated `app/features/two-factor/` routes exist today; any future UI should be documented here when added.

links:
  planning_docs:
    - .ai/features/two-factor/README.md

  implementation_docs:
    - back-auth/features/two-factor/feature.yaml

---

Metadata:
- Last Updated: April 22, 2026
- Maintainer: Engineering Team
- Status: Active
