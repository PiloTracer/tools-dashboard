#
# Shared environment variables
#

# Base URL for the authentication service (back-auth) exposed to front-public.
# When running via docker-compose, point to the internal service hostname.
# Override to http://localhost:8101 only if you run front-public outside Docker.
BACK_AUTH_BASE_URL=http://back-auth:8001

# Public URL for the Remix app (used for verification + redirects)
PUBLIC_APP_BASE_URL=http://epicdev.com/app
PUBLIC_APP_BASE_PATH=/app
VERIFICATION_PATH=/features/user-registration/verify

# Security + cookie configuration (production hardened)
REGISTRATION_PASSWORD_MIN_LENGTH=12
REGISTRATION_CSRF_COOKIE_SECURE=false
REGISTRATION_CSRF_COOKIE_SAMESITE=Lax
REGISTRATION_CSRF_COOKIE_MAX_AGE=900
SESSION_COOKIE_NAME=td_session
SESSION_COOKIE_SECURE=false
SESSION_COOKIE_SAMESITE=Lax
SESSION_COOKIE_MAX_AGE=604800

# Email delivery settings (configure with your SMTP provider)
MAIL_HOST=smtp.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
MAIL_PORT=587
MAIL_USERNAME=apikey
MAIL_PASSWORD=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
MAIL_SENDER=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx@xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.com
MAIL_USE_TLS=true

# Verification token lifetime (minutes) and refresh TTL (days)
REGISTRATION_TOKEN_TTL_MINUTES=15
REFRESH_TOKEN_TTL_DAYS=7

# Cassandra auth event logging (leave blank to disable in local dev)
CASSANDRA_CONTACT_POINTS=
CASSANDRA_PORT=9042
CASSANDRA_KEYSPACE=auth_events

# Google OAuth client configuration
GOOGLE_OAUTH_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
GOOGLE_OAUTH_CLIENT_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
GOOGLE_OAUTH_REDIRECT_URI=http://epicdev.com/app/features/user-registration/verify?provider=google
GOOGLE_OAUTH_SCOPES=openid email profile
# Optional extras:
# GOOGLE_OAUTH_ALLOWED_DOMAINS=example.com
# GOOGLE_OAUTH_BUTTON_TEXT=Continue with Google
