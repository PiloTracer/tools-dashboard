name: feature-registry
version: 1.0.0
description: Standalone FastAPI service — planned central catalog for apps and feature contracts (scaffold)
status: scaffold
owner: Engineering Team

services:
  feature-registry:
    entrypoints:
      - feature-registry/main.py
      - feature-registry/registry.py

    http:
      - path: /health
        method: GET
        description: Service liveness

    context_docs:
      - feature-registry/CONTEXT.md

notes:
  - No product `front-*` or `back-api` feature package; this is its own deployable service directory at repo root.

links:
  planning_docs:
    - .claude/features/feature-registry/README.md

---

Metadata:
- Last Updated: April 22, 2026
- Maintainer: Engineering Team
- Status: Scaffold
